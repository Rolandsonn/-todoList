import React from "react";
import Input from "../Input";

const Modal = ({ handlechangeCgeck, handleClickAdd }) => {
  return (
    <div>
      <Input handlechangeCgeck={handlechangeCgeck} />
    </div>
  );
};

export default Modal;
